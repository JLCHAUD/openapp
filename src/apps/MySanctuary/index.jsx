// Placeholder — will be replaced in a later task
export default function MySanctuary({ onBack }) {
  return (
    <div style={{ padding: '2rem', color: '#fff' }}>
      <button onClick={onBack}>← Retour</button>
      <p>My Sanctuary — à venir</p>
    </div>
  )
}
